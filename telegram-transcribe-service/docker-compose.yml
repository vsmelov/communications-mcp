services:
  worker:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATA_DIR: /data
      PORT: 8077
    volumes:
      - ./data:/data
    ports:
      - "8077:8077"
    extra_hosts:
      - "host.docker.internal:host-gateway"
